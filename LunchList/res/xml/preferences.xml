<PreferenceScreen
    xmlns:android="http://schemas.android.com/apk/res/android">
    <ListPreference
        android:key="sort_order"
        android:title="Sort Order"
        android:summary="Choose the order the list uses"
        android:entries="@array/sort_names"
        android:entryValues="@array/sort_clauses"
        android:dialogTitle="Choose a sort order" />
    <ListPreference android:entries="@array/default_type" android:entryValues="@array/default_type" android:dialogTitle="Default Type" android:title="Default Type" android:key="default_type" android:summary="Choose the default restaurant type"/>
</PreferenceScreen>